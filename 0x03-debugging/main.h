#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

void positive_or_negative(int i);
int largest_number(int A, int B, int C);
void preint_remaining_days(int month, int days, int year);
int convert_day(int month, int day);

#endif /* MAIN_H */
